# SESSION 32 MASTER HANDOVER (final — L11, L12, L13 all complete)

**Rangkai Protocol — Multi-Marketplace B2B Cross-Border Infrastructure**

Date: July 7, 2026 | Sessions Completed: 1–32 | Handover for: Session 33
Project Status: L11, L12, L13 all done, tested, pushed. Next: L14 (buyer-visible shipping options on product page).

---

## Read this first

**Check before starting Session 33:** confirm `docs/TECH_DEBT.md` was actually pushed. It was accidentally left out of the L13 commit's `git add` (a real slip this session — commit said "5 files changed" but 6 were modified/new; `TECH_DEBT.md` was the one left behind) and needed a follow-up commit. Run `git log --oneline -5` and check for a commit like `docs(tech-debt): L11/L12/L13 status updated...` — if it's not there, the tech debt file on GitHub is stale relative to what's described in this handover.

---

## Why Rangkai Exists

Pam's 2025 Medium piece **"When Global Trade Leaves Small Businesses Behind"**
(https://medium.com/@christinepamela/when-global-trade-leaves-small-businesses-behind-d9e31b6c292a).
Global trade infrastructure was built for large corporations. Rangkai is infrastructure for the small businesses left out — a protocol that enables marketplaces, not a marketplace itself. Federation is the point, from day one.

---

## About Pam (User)

Solo founder, self-funded, has vendors lined up to test. 2021 Medium piece on logistics (https://medium.com/@christinepamela/logistics-for-startups-f27df1e1e3a) — canonical framework: 6 segments A–F, Incoterm groups, landed cost calculation.

### How Pam Works

- Self-rates **2/10 software skills** but is architecturally sharp — when something seems overcomplicated, she's often right to push back (example this session: the RFQ destination-country requirement seemed to need a whole new UI; the real fix was making one field optional).
- Prefers **exact code, exact file path, exact line** — "find this → replace with this," step by step, no bullets for code instructions.
- **Windows command prompt only** (NOT PowerShell).
- Pastes files directly into chat — **ask for the file you need**, don't guess. She will catch it via `dir` if a guessed path is wrong — trust her verification over assumption every time (happened twice this session: once with a stale file-header comment, once with a `git add` that missed a file).
- No reliable way for Claude to self-report context capacity — write a handover proactively when the conversation has clearly gotten long, not tied to a specific percentage.
- Cross-checks every file change before proceeding — catches real bugs and slips this way, including catching a missed file in a git commit this session. Keep this habit reinforced.
- Wants ONE commit per logical change, only after full testing passes.
- Wants tech debt entries added the same turn something is deferred, with exact find/replace text.
- **Always verify a commit's file list actually matches everything intended** before telling her it's done — check `git status` output against the `git add` command line by line, don't just assume.

### Tech Debt Stance

Small fixes immediate. Architectural items go in `docs/TECH_DEBT.md`, scheduled, never deleted — `✅ Fixed S<N>` when done. Reopened items say plainly they were marked done incorrectly before.

### The Four Types of Users

1. **Marketplace entrepreneurs** — spin up Rangkai-powered marketplaces.
2. **Buyers** — find products, place orders, pay, track.
3. **Sellers** — list products, manage orders, ship. *(Codebase still uses `/app/vendor/...`, `VendorSidebar`, etc. — terminology migration incomplete, cosmetic.)*
4. **Logistics** — see opportunities, quote, accept, deliver. KYC-mandatory.

---

## Server Start Commands

**Terminal 1 — Protocol API (3000):** `cd C:\Users\chris\marketplace-protocol` → `npm run dev:api`
**Terminal 2 — Stripe listener (optional):** `stripe listen --forward-to localhost:3000/api/v1/payments/stripe/webhook`
**Terminal 3 — Rangkai marketplace (3001):** `cd rangkai-marketplace` → `npm run dev`
**Terminal 4 — Logistics marketplace (3002):** `cd logistics-marketplace` → `npm run dev`

**Critical:** `packages/sdk` compiles via `tsc` to a `dist` folder that apps actually import. Any edit under `packages/sdk/src/` requires:
```
cd C:\Users\chris\marketplace-protocol\packages\sdk
npm run build
```
...then restart the dependent app(s). Symptom when forgotten: `sdk.X.Y is not a function`. Cost real debugging time in S32 — don't rediscover it.

### Always Check Before Testing
Supabase pauses after ~1 week inactivity — check supabase.com/dashboard.
`curl http://localhost:3000/api/v1/health`; confirm `localhost:3001`/`localhost:3002` load.

### Browsers
Firefox = Seller, Chrome = Buyer, Edge = Logistics.

---

## Architectural Principles (Always-On)

1. Protocol is passive infrastructure — 0.5%+0.5% from payouts at escrow release, never added to buyer's total.
2. Federation is the goal from day one.
3. Cryptographic identity (secp256k1) — protocol never holds passwords/keys/KYC docs.
4. Nostr is plumbing, Rangkai owns UX and search.
5. Small business first — hide complexity. If something seems hard, check for the simpler framing first.
6. Bitcoin is the invisible rail — v1 pays out BTC/USD directly, invisible conversion is v1.5 (R11).
7. Terminology: seller, buyer, logistics — not vendor, provider, courier.

---

## What Happened in Session 32 (complete)

### L11 — Path B2 (buyer arranges own logistics) — ✅ DONE, TESTED, PUSHED

Schema: `products.require_logistics_quote`, `orders.own_logistics` (both bool, default false).
6 files, one commit (`e046940`). All 6 test steps passed.

### Documentation — Section 13 corrected, rest deferred to after L16

`LOGISTICS_ARCHITECTURE.md` Section 13 escrow formula fixed, committed separately. Section 3 (fee-as-visible-line-item) and the unmerged "Architecture doc additions" block remain open, tracked in L17, explicitly deferred to after L16.

### L12 — Product creation Incoterm/HS code/logistics-quote toggle + RFQ auto-broadcast — ✅ DONE, TESTED, PUSHED

**Design correction worth remembering:** original plan assumed the RFQ broadcast needed a pinned destination country at publish time (no buyer exists yet, so this seemed to need a whole destination-picker UI). Pam pushed back — compared to Shopify-style shipping profiles, no destination needs pinning before a buyer exists. Fix: made `destination_country` optional on the broadcast; omitted means "any destination this provider serves," matched via the provider's declared routes. Small targeted fix, not a new UI flow.

**11 files + 1 migration, one commit, pushed.** Full file list and per-file description preserved in git history / prior handover drafts — key files: `packages/sdk/src/types.ts`, `packages/sdk/src/modules/catalog.ts`, `rangkai-marketplace/components/vendor/ProductForm.tsx`, `rangkai-marketplace/lib/utils/incoterms.ts` (new), `rangkai-marketplace/app/products/[id]/page.tsx`, `src/core/layer1-catalog/types.ts`, `src/core/layer1-catalog/product.service.ts`, `src/api/routes/catalog.routes.ts`, `src/api/routes/logistics.routes.ts`, `packages/sdk/src/modules/logistics.ts`.

**Bugs hit and fixed, worth remembering the pattern:**
1. First test used the wrong code path (list page's eye-icon toggle bypasses `ProductForm.tsx` entirely — only Edit → Publish Product inside the form triggers the RFQ broadcast)
2. `sdk.logistics.requestQuote is not a function` — SDK needed rebuilding, see Server Start Commands note above
3. 500 error on RFQ broadcast — root cause was `quote_requests.destination_country` still having a DB-level `NOT NULL` constraint even after the Zod schema was relaxed to optional. **Lesson: when relaxing a validation rule, check the database constraint too, not just the application-layer schema.**

All 7 test steps passed. Step 6 (opportunities matching) was **logic-verified only, not empirically confirmed live** — the logistics-marketplace opportunities page crashes on unrelated pre-existing bugs (see B15), and BitHaul's auth token couldn't be located to test the endpoint directly. Tracked as D21, folded into L16's testing.

### L13 — Seller quote review UI on product page — ✅ DONE, TESTED, PUSHED

**What was built:**
- `src/core/layer3-logistics/quote.service.ts` — new `getQuotesWithProvidersForProduct(productId)` method, mirroring the existing order-scoped `getQuotesWithProviders()`
- `src/api/routes/logistics.routes.ts` — new `GET /logistics/quotes/product/:productId` with ownership check (verifies `vendor_did` matches); also **closed a pre-existing security gap**: the `POST /quotes/:id/accept` route had a `// TODO: Add ownership check` comment with no actual check — any authenticated user could accept any quote. Now verifies the accepting user owns the product (product-type quotes) or is buyer/vendor on the order (order-type quotes) before allowing accept.
- `packages/sdk/src/modules/logistics.ts` — added `getQuotesForProduct(productId)`
- `rangkai-marketplace/app/vendor/products/[id]/quotes/page.tsx` (new) — seller-facing page: lists pending quotes (provider name, rating, price, days, method, insurance badge, valid-until date) with an Accept button; shows accepted quote in a distinct green section once chosen
- `rangkai-marketplace/app/vendor/products/[id]/edit/page.tsx` — added a "View logistics quotes →" link

**Important file-path lesson from this session:** `quote.service.ts`'s own header comment claimed `// Path: src/services/quote.service.ts` — this was **stale/wrong**. The actual file, confirmed via `dir`, is at `src/core/layer3-logistics/quote.service.ts` (matching the import path used in `logistics.routes.ts`). Pam caught this by running `dir` on both candidate paths rather than trusting either the comment or Claude's initial guess. **Lesson: header comments can go stale after refactors — trust import statements and `dir` verification over comments.**

**Tested via direct SQL insert** (not the logistics-marketplace's own quote-submission UI, which is currently blocked by pre-existing bugs B5/B15): inserted a test quote from BitHaul against the "custom" product ($15 USD, 5 days, insurance included), confirmed it displayed correctly on the new seller page, clicked Accept, confirmed it moved to the accepted section and the DB showed exactly one `accepted` row for that product.

### Tech Debt — Updated This Session

- **B14** — ✅ Fixed (logistics-required error shows product name not ID)
- **B15** (new) — logistics-marketplace opportunities page crashes on real data (data-shape mismatch, pre-existing, bundled into L16)
- **D19** — origin country free text vs ISO code validation gap (pre-existing, tracked)
- **D21** (new) — L12 opportunities-matching not empirically confirmed live, only logic-traced (folded into L16 testing)
- **L11, L12, L13** — all marked ✅ done/tested/pushed with full detail
- **R21** — L12 "Option B" (hard-block publish until real quote exists) deferred until after L13 — **now that L13 exists, this may be worth revisiting in a future session** as originally planned
- Last-updated date bumped to Session 32

**⚠️ Verify this was actually pushed — see "Read this first" at top of this handover.**

---

## Full B1 Logistics Job List Status

| # | Job | Status |
|---|-----|--------|
| L1–L10 | Schema + backend | ✅ Complete (S30–S31) |
| L11 | Path B2 — buyer arranges own logistics | ✅ Done, tested, pushed S32 |
| L12 | Product creation Incoterm/HS/toggle + RFQ broadcast | ✅ Done, tested, pushed S32 |
| L13 | Seller: quote review UI on product page | ✅ Done, tested, pushed S32 |
| L14 | Buyer-visible shipping options (Incoterm plain-language helper already built in `lib/utils/incoterms.ts`, ready to reuse) | ❌ **Next up** |
| L15 | Checkout: unified total + buyer override | ❌ Not started |
| L16 | Logistics-marketplace opportunities dashboard (also fixes B5, B15) | ❌ Not started |
| L17 | Correct `LOGISTICS_ARCHITECTURE.md` section 13 | 🟡 Partial — Section 3 + doc-additions merge deferred to after L16 |

---

## Git State

Commits this session, in order:
1. `docs(logistics): correct section 13 escrow split formula...`
2. `L11 Path B2 — require_logistics_quote gate + own-logistics opt-out. Tested S32.` (`e046940`)
3. `docs(tech-debt): L11 done, B14 fixed, L17 partially reopened (S32)`
4. `L12: Incoterm/HS-code/logistics-quote-toggle on product creation + RFQ auto-broadcast. Tested S32.`
5. `L13: seller quote review UI — accept quotes on product page, with ownership checks. Tested S32.` (`217bca6`)
6. **Needs verification:** `docs(tech-debt): L11/L12/L13 status updated...` — check this landed, see top of handover

---

## Database State

**Identities:** Bitshop (seller, KYC) `testBit2@test.com`; Bitty Buy (buyer, KYC) `bittybuy@bitbit.com`; BitHaul (logistics, KYC) `BitHaul@123.com`, provider ID `4ac9c4ca-8022-4b9d-b12e-ce5200f39af1`.

**Products:** "handmade leather boots" (`6652da96-3bad-4932-8a12-e56659bc0881`) and "custom" (`bad3b0e0-5b97-413a-8c5c-cfd3812a51bd`) — both Bitshop's, both Active.

**shipping_quotes:** contains one test row (id `8a54f596-b31e-4d54-9f10-9d1ad44cfb48`) — BitHaul's $15 USD quote on "custom," status `accepted`. This is real test data sitting in the product's accepted-quote slot; if L14 testing needs a clean slate, may want to reset this row's status or remove it first.

**quote_requests:** test row `abc27f8b-c112-4aea-9e7a-a1c26db5d933` (product "custom", origin MY, destination null, incoterm DDP, status open) plus an older unrelated row (product "handmade leather boots", destination DE).

**logistics_providers:** BitHaul has `routes = [{"origin_country":"MY","destination_country":"SG"}]`, `incoterms_supported = ['DAP','DDP']`.

**Schema added this session:**
```sql
ALTER TABLE products ADD COLUMN require_logistics_quote BOOLEAN DEFAULT false;
ALTER TABLE orders ADD COLUMN own_logistics BOOLEAN DEFAULT false;
ALTER TABLE products ADD COLUMN hs_code TEXT;
ALTER TABLE quote_requests ALTER COLUMN destination_country DROP NOT NULL;
```

---

## Test Accounts

Seller (Firefox, 3001): Bitshop, testBit2@test.com / bitshop123
Buyer (Chrome, 3001): Bitty Buy, bittybuy@bitbit.com / bittybuy123
Logistics (Edge, 3002): BitHaul, BitHaul@123.com / BitHaul123

---

## L14 — Scoping Notes for Next Session

**Goal:** Product page shows buyer-visible shipping options — provider name, price, days, Incoterm translated into plain language ("Door to door, you pay import duties on arrival" for DAP; "Fully delivered, all duties included" for DDP).

**What already exists to build on:**
- `lib/utils/incoterms.ts` — `getIncotermExplanation()` already built (L12), currently only used in the Shipping info block, ready to reuse for actual shipping option cards
- An accepted quote already exists in the DB from L13 testing (BitHaul, $15, "custom" product) — can be used directly as real test data for L14 without needing a fresh setup
- `app/products/[id]/page.tsx` — the buyer-facing product detail page already has a Shipping section; L14 likely extends this rather than replacing it

**Not yet confirmed, worth checking first:**
- Whether `sdk.catalog.getById()` (used by the buyer-facing product page) returns the accepted quote data at all, or whether a new endpoint/join is needed to fetch "this product's accepted shipping quote" alongside the product itself
- How this connects to L15 (checkout) — L14 is really "show it," L15 is "let the buyer pick it and carry it into the order"

**Suggested first step:** check what `GET /catalog/products/:id` currently returns — if it's just the raw `products` row (per `product.service.ts`'s `getProductById()`), it won't include quote data, meaning the buyer-facing page will need a second fetch to `GET /logistics/quotes/product/:productId` (the same endpoint L13 just built) to get the accepted quote, filtered client-side or server-side for `status === 'accepted'`.

---

## Troubleshooting Reference

### API Server Won't Start
`Router.use() requires a middleware function but got a Object` → empty/misconfigured route file in `routes/index.ts`.

### "TypeError: fetch failed" on Startup
Supabase paused — restore at supabase.com/dashboard.

### SDK changes not taking effect
Rebuild: `cd packages\sdk && npm run build`, then restart the dependent app(s).

### Seller Logs In, Can't See Their Products
`SELECT did, email FROM identities WHERE email = 'their@email.com'` — should be exactly one row.

### Mnemonic Rotation
1. `node -e "const bip39 = require('bip39'); console.log(bip39.generateMnemonic(256));"`
2. Replace `BITCOIN_MNEMONIC` in `.env`, set `BITCOIN_START_INDEX=0`, restart API server
3. `DELETE FROM bitcoin_payment_addresses WHERE order_id LIKE 'burned-placeholder-%';`
4. Store mnemonic securely offline — never in version control.

---

**END OF SESSION 32 HANDOVER (FINAL)**

L11: ✅ | L12: ✅ | L13: ✅ | Next: L14 — verify tech debt push landed first, then check what `GET /catalog/products/:id` returns before writing any code